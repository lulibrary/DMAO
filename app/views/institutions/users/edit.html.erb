<% content_for :title, 'Edit Institution User' %>

<%= form_for @institution_user, url: { action: :update, institution_identifier: @institution.identifier } do |f| %>

    <%= render 'form', f: f %>

<% end %>
